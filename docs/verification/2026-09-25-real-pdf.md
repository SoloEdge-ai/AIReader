# 私有 516 页 PDF 本机验收（阶段 5，部分）

验证提交：`ec4c0eb`。在该提交上执行以下命令；`<本机 PDF 路径>` 仅代替用户提供的私有绝对路径，其他参数均为实际值：

```powershell
node --import tsx scripts/manual-pdf-acceptance.ts '<本机 PDF 路径>' 14 1
node --import tsx scripts/manual-pdf-acceptance.ts '<本机 PDF 路径>' 14 1.5
node --import tsx scripts/manual-pdf-acceptance.ts '<本机 PDF 路径>' 14 2
```

脚本启动真实 Core 和 Edge，经界面文件输入及 Core HTTP 导入到系统临时目录，并等待 PDF.js 报告第 14 页渲染完成、Canvas 尺寸非零；结束后删除自己的临时书库。截图仅写入忽略的 `.local/screenshots`，不提交 PDF、提取文本或截图。此次输入是用户此前指定的 516 页技术书；只记录元数据和时长，不公开其路径与内容。

环境：Windows 11 `10.0.26200`、Intel Core Ultra 7 155H、32 GiB RAM、Node 24、Edge 无头浏览器。三次独立导入，页面 14 在 1440×900 CSS 视口下打开，分别用浏览器设备像素比 1、1.5、2：

| 设备像素比 | 界面导入至 ready | 首张页面完成渲染¹ | 跳转并完成页 14 渲染 | 页 14 Canvas |
| --- | ---: | ---: | ---: | --- |
| 1 | 5.39 s | 4 ms | 277 ms | 654×926 |
| 1.5 | 6.03 s | 4 ms | 230 ms | 982×1389 |
| 2 | 5.99 s | 3 ms | 275 ms | 1309×1852 |

¹ 首张页面可能已在界面导入期间完成渲染，表中仅测量从 ready 到确认完成所需时间，不能解读为总渲染时间。

Core 进程在三次打开时 RSS 为 204–217 MiB。人工检查最新截图：第 14 页正文、图示、页码、工具盘均可见，没有明显文字块错位或图像缺失。此检查使用**浏览器设备像素比**，不等同于 Windows 桌面 100%／150%／200% 显示缩放验收；未测长时间滚动、连续绘画、安装版或 AI。阶段 5 仍未完成。

2026-09-25 回归：在 `507500c` 的应用构建上使用同一私有 PDF、同一脚本与页 14、设备像素比 2 再次验证；导入至 ready 6.09 s、跳页渲染 276 ms、Canvas 1309×1852、Core RSS 203 MiB。人工查看忽略目录中的截图，正文与图示均完整，工具盘仍可见。此后 `7185b83` 只修改工具盘测试页验收逻辑，未修改应用构建。仍不能将设备像素比检查等同于 Windows 系统显示缩放或长期性能验收。
