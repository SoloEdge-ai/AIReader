# 三栏阅读布局验证

状态：阶段 4 的布局切片；画布卡片／笔迹尚未进入左侧材料列表，不能视为完整材料导航。

- 先用真实阅读页浏览器测试确认旧界面无法让材料笔记与右侧问答同时存在。
- 现将批注／笔记列表及编辑放在左侧“材料”，右侧专用于问答；中间仍是同一个 PDF／白板。已有 Note 草稿会话没有复制一份。
- 左侧宽度 220–320 px、按书持久化。窄窗打开材料时收起问答，打开问答前提交材料编辑草稿；保存失败不能静默切换。旧 `panel: notes` 偏好在打开书籍时作界面映射。
- 浏览器定向验证：1280 px 同时显示左材料／中央 PDF／右问答、900 px 交替抽屉、左栏鼠标拖动及键盘 Home/End 后重开保持、旧 `panel: notes` 偏好映射；生成 PDF 的截图在本地 `test-results/materials-and-chat.png`，未提交用户书籍内容。
- 切换材料标签时的保存失败用例确认：保持原标签和未保存草稿；重试保存后才可切换。
- `pnpm typecheck`、`pnpm test`（29 文件／47 项）、`pnpm test:reader-ui`（25 项）通过；生产构建及 bundled Core smoke、聊天 HTTP/UI 验收、Windows Setup／Portable 本地构建通过。最后只调整导航按钮名称和测试夹具记录的实体 ID，精确提交的双产物仍由 PR CI 验证。
