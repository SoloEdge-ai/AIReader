# 区域摘录与兼容命令的重试身份

状态：命令重试切片 `48fb2a2` 与仓储切片 `ead6d14` 的 Windows CI 均通过；资源读取切片本机验证、尚未推送。

真实 Core HTTP 测试先证明：以相同 commandId 把已创建图片摘录的标题换成另一内容，旧入口仍返回 201，无法区分合法重试和 ID 复用。修复后新 v1 工作区命令回执保存输入摘要；区域摘录以经过协议校验的完整请求（包含截图数据、来源、版本和标题）计算摘要。相同请求重试仍复用已有卡片，不重复登记资源；同 ID 不同内容返回 409 COMMAND_ID_REUSED。校验在异步图片写入后的工作区事务内再执行一次，因此同时到达的请求也不能绕过。已存在、没有摘要的旧兼容回执保留原行为，不自动改写。

本地 `workspace-region`、v1 工作区 HTTP 和 v2 命令回执定向测试通过。区域测试还同时发送两次相同的新摘录请求，两个响应定位同一资源，书籍只新增一份资源记录和一个 PNG 文件，无多余临时图片；为保持后续伪造来源检查有效，该测试的 expectedVersion 随新增卡片从 1 调整为 2。v1 响应仍返回当前工作区，不等于 v2 的不可变差量回执；后续应将区域资源纳入 v2 的事务回执模型。

上一提交 `05a4de6` 的 Windows PR 构建在画布对象验收首次保存文字对象时超时；前序 `dfe2f4b` 对应检查通过，同一步本机独立连续运行五次通过。因此暂不能证明是产品问题或仅是 CI 抖动。本次给验收脚本增加首个对象未保存时的修订号、编辑器内容、工作区错误、命令请求状态和页面异常诊断，并将该步等待上限设为 10 秒。该诊断只处理 CI 生成的 PDF 与文本。

`48fb2a2` 的 [Windows CI 36130472719](https://github.com/SoloEdge-ai/AIReader/actions/runs/36130472719) 全绿，包含此前超时的画布对象步骤、打包桌面、安装／卸载和 SHA256。一次成功不能证明之前超时的根因；保留失败诊断以便再现。

仓储切片 `ead6d14` 把仍存 records 的兼容回执、区域资源元数据及 PDF 页面边界缓存集中到 `WorkspaceRepository`，调用处不再直接拼接通用记录键。归档恢复也使用同一资源登记方法；同一资源 ID 不可覆盖已有书籍的登记。本机通过 typecheck、38 文件／60 项 Core 测试和生产构建；[Windows CI 36131570196](https://github.com/SoloEdge-ai/AIReader/actions/runs/36131570196) 全绿。

资源读取切片增加了一个真实 Core HTTP 红灯测试：删除区域 PNG 的登记但保留文件时，旧工作区打包仍成功（返回 200）。现在工作区打包和源卡片笔记导出共用按书籍校验的区域资源读取入口；缺登记的两种导出均返回 400，恢复登记后可成功导出。本机通过 typecheck、38 文件／60 项 Core 测试与归档、笔记导出定向测试；尚待 Windows CI。
