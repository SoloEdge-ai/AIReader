# 聊天流与工具运行写入交错

状态：本地验证通过；尚未推送到 Windows CI，也不表示编程工具沙盒可用。

`ChatService` 的流式回调保留创建轮次时的内存对象；工具运行在同一轮次追加或完成时写入数据库。此前后来的流式保存会用旧对象整行覆盖，把工具记录清空。新增真实 SQLite 仓储测试先复现此失败，再让 `ChatRepository` 按书籍管理工具记录：流式保存沿用持久化的工具列表，并将合并后的轮次用于事件广播；工具追加和完成读取当前轮次再写入，因此不会擦掉已更新的回答。跨书轮次不可读取或追加工具，已有轮次也不能被同 ID 的其他书籍／会话写入覆盖。`BookTools` 不再直接读写通用 Storage 的 turn 记录。

定向 `chat-repository`、流式聊天和 Core HTTP 隔离测试已通过；在修复事件广播前，新增回归也先失败。聊天 HTTP 测试现在还通过真实 WebSocket 订阅，在进行中的轮次写入工具记录、取消回答后核对事件和 HTTP 快照都保留该工具。本地 `pnpm typecheck`、38 文件／59 项 Core 测试及生产构建／bundled Core smoke 在最终改动后通过；Windows CI 尚待提交。这里没有改变沙盒验证策略；本机不满足隔离要求时编程工具仍禁用。
