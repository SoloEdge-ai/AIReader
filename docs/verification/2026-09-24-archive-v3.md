# 工作区归档 v3 验证

状态：阶段 4 的归档格式切片；不代表完整阶段验收。

- 测试先期望导出 v3、拒绝 v1/v2、拒绝未知 v4，原实现导出 v2，红灯确认。
- Core 导出及版本检查统一读取 `build/format-versions.json` 的 archiveVersion=3，发布元数据使用同一文件。v1/v2 和较新版本均在恢复前明确拒绝，不会部分创建副本。
- 真实 Core HTTP 测试恢复 v3 独立副本，核对 PDF、Note、批注、卡片、关系、相机、区域 PNG、指纹和资源 ID 重映射；恶意路径、篡改指纹、畸形压缩长度仍拒绝。
- v3 清单必须显式包含工作区 formatVersion、layoutVersion 和 objects；旧形状的清单即使伪造 version=3 也被拒绝。该约束先由失败用例确认，再用严格归档 schema 修复；数据库读取仍允许自身既有的兼容默认值。
- 使用项目 Node 24 执行 `pnpm test`：28 文件／45 测试通过；`pnpm typecheck`：通过。`pnpm desktop:windows`：通过，包含生产构建、打包 Core 启动、已认证 HTTP smoke、当前用户 Setup 和 Portable EXE；构建元数据显示 archiveVersion=3，提交 SHA 为构建时 HEAD，workingTreeDirty=true。系统默认 Node 22 缺少 SQLite FTS5，不能用来运行这组测试。
- 首次完整浏览器运行揭示 Node 直接加载 JSON 格式版本缺少 import attribute；增加 `with { type: "json" }` 后 `pnpm test:reader-ui` 20 项通过。
- 用户旧 v1/v2 `.aireader` 包在此分支不能直接恢复；这与已接受的重构计划一致，旧包须用旧版处理。
