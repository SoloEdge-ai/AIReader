# 工作区增量回执切片

状态：阶段 2 的命令基础切片，尚未完成统一 Note／placement 或 v5 存储。接续 1f1c66d，仍在同一 Draft PR。

## 行为验证

- 新 Core HTTP 测试先收到 404，实施 v2 命令后通过。覆盖原始回执跨重启重放、同 ID 不同载荷、伪造摘要、版本冲突、删除关系及逆操作、失败事务回滚。
- renderer 使用真实 Core 验证提交成功但响应丢失时，另一窗口更新后重试仍保留本地新稿并报告冲突，不用最新远端状态掩盖版本差异。
- HTTP 测试补充跨书和页面越界对象拒绝。测试只使用生成 PDF 与隔离 SQLite，不含用户材料。

## Standards 审查

首轮指出区域卡片 inverse 无法通过受控创建校验。新增 HTTP 测试先复现 400，再修复 Core 来源留存与合法恢复；篡改 inverse 的原文矩形仍拒绝。复核无遗留阻断项（静态复核，非独立测试运行）。

## Spec 审查

首轮另指出级联删除的 inverse 会超过原有 2000 条上限。测试删除 2000 个对象及 999 条关系，先复现 inverse 提交 400，再以 v2 上限和验证入口修复。补充 100 张大卡片的逆操作超 8 MiB 测试：原操作被拒且内容完整回滚。复核无新增明确问题。

## 最终验证与限制

- 类型检查、协议／引擎边界及文档链接门禁通过。
- 修复审查问题后完整 Vitest：26 文件／43 测试通过（61.12 秒）。
- 修复后 Playwright：13 项通过（22.2 秒），包含共享笔记、工作区重试、工具盘和菜单。
- 最终 Windows Portable／当前用户 Setup 构建通过；打包程序的文本、形状、跨页笔迹、保存后重新打开检查通过。使用生成 PDF 和独立 .local 测试数据目录。
- 本地包为 0.1.0 development，基于 1f1c66d 的本次工作树，workingTreeDirty=true；它不是正式或预览发版。构建环境 Windows 11 10.0.26200，显式 Node 24.19.0；嵌套 pnpm shim 的 Node 22 engine 警告、已有 bundle/PDF.js 样式资源警告仍存在。

未安装、未清理用户数据、未发布预览。区域资源和笔记仍使用既有入口；增量回执按实体保存，但工作区本体仍是 v4 JSON 记录，不能称为已完成规范化存储。
