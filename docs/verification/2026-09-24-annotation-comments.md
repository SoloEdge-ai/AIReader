# 标注与按需评论验证

状态：接续 d65d642 的阶段 2 切片，不代表 Note／placement 或统一 v2 编辑历史完成。

## 行为与测试

- Core HTTP 测试先验证高亮没有 noteId 和自动空笔记，旧实现失败；改为显式评论接口后通过。评论重试复用已有内容，跨书／已删除源标注拒绝。
- 删除源标注保留评论和来源；删除笔记解除活跃指针但保留标注；旧笔记恢复不抢占后来创建的评论。来源经过 Core 列表投影，不接受正文更新覆盖。
- 工作区归档恢复无评论标注，以及引用已删除源标注的评论／区域图片；恢复后 ID 重映射，跨书图片读取拒绝。归档仍为 v2，完整 v3 尚待实施。
- 新界面测试通过真实 Core 创建标注，并验证无空编辑器、“写评论”、独立删除与恢复。模拟 DELETE 响应延迟时继续输入，先复现来源删除状态被脏草稿挡住，再修正为保留正文基线、同步只读来源。
- 首轮新用例使用 Vite 的相对 HTTP 地址导致响应解析失败，已改为测试 Core 地址；并修复既有展开编辑用例对侧栏默认收起的假设。没有放宽产品断言。
- 类型、依赖及文档检查通过；完整 Vitest 26 文件／43 测试通过（65.03 秒）；最终 Playwright 14 项全部通过（22.5 秒）。
- Windows Portable／当前用户 Setup 构建通过；本地开发版 0.1.0，元数据 d65d642 + dirty。不是正式 Release。打包后 1x DPI 批注、按需评论、删除来源保留笔记、自动保存／失败恢复、退出保护、重新打开、跨页、旋转／缩放锚点检查通过。
- 检查生成样本截图 `test-results/annotation-comment-source.png`：来源删除提示、禁用的颜色控件、保留的正文及操作可见。并发失败样本同时留下全局错误提示，不将此图当作最终全 UI 视觉验收。未覆盖本轮全尺寸／全 DPI。
- 构建保留既有 pnpm shim Node 版本告警、PDF.js SVG 资源及 chunk 大小告警；桌面脚本保留 shell 参数弃用告警。测试进程使用 Node 24.19.0，桌面使用 Electron 运行时。

## Standards

独立静态审查未发现硬性违规或需提出的代码异味；要求同步的生命周期文档已更新。

## Spec

首次发现 1 个 P2：脏草稿阻挡 annotationSource 的删除状态刷新。已增加失败后通过的真实 UI 回归，复核无新增明确问题。审查不是测试运行的替代。

## 范围与限制

未安装、未清理本机数据、未发布预览。DB v5、归档 v2、最高 API v2（Note 仍是 v1）。个人卡片与富文本 Note 仍分开；Note／placement、跨实体 v2 历史、三栏 UI 和完整 DPI／性能验收仍未完成。构建使用隔离生成样本，不使用用户 PDF。
